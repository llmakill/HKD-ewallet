<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cbdc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.NoTitleBar">
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.login.RegActivity" />
        <activity android:name=".ui.login.MainActivity" />
        <activity android:name=".ui.login.AddwalletcardActivity" />
        <activity android:name=".ui.login.ExchangeActivity" />
        <activity android:name=".ui.login.ChoosewalletActivity" />
        <activity android:name=".ui.login.ExchangeSuccess" />
        <activity android:name=".ui.login.ExchangeFail" />
        <activity android:name=".ui.login.TransactionActivity" />
        <activity android:name=".ui.login.TransactionDetailActivity" />
        <activity android:name=".ui.login.AccountManagementActivity"/>
        <activity android:name=".ui.login.PayMethodActivity" />
        <activity android:name=".ui.login.ExportActivity" />
        <activity android:name=".ui.login.QRCodeActivity"/>

    </application>
    <uses-permission android:name="android.permission.INTERNET"/>
<!--    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />-->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />

</manifest>